rootProject.name = "pf2e-return-of-the-runelords-conversion"

// TODO: remove this later on when plugin is published
pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
    }
}